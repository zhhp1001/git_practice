# This is a file for learning git command
There are four area in git system:
- working directory
- staging area
- local repo(HEAD)
- remote repo(MASTER)

Lists version history for a file, beyond renames 
`$ git log --follow [file]`
